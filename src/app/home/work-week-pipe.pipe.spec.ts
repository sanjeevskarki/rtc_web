import { WorkWeekPipePipe } from './work-week-pipe.pipe';

describe('WorkWeekPipePipe', () => {
  it('create an instance', () => {
    const pipe = new WorkWeekPipePipe();
    expect(pipe).toBeTruthy();
  });
});
